# 星云寻知可选构建配置示例
#
# 使用方法：
#   1. 将本文件复制为项目根目录下的 .env。
#   2. 只修改需要定制的配置，未填写的配置使用项目内置默认值。
#   3. 命令行环境变量优先级高于 .env，.env 优先级高于内置默认值。
#   4. 执行 `corepack pnpm@11.24.0 desktop:package` 一键完成当前平台打包。
#
# .env 仅供开发和构建阶段读取，不会被提交，也不得进入安装包、
# 内核、诊断包或 BUILD-INFO.<target>.json。此文件不应填写 API Key 等敏感信息。

# -----------------------------------------------------------------------------
# 应用信息
# -----------------------------------------------------------------------------

# 用户可见的应用名称。
# 用于窗口标题、原生菜单、关于页面、安装包名称和应用目录名称。
DESKTOP_APP_NAME=Xingyunxunzhi

# 桌面应用版本，必须符合 SemVer。
# 默认和示例版本固定为 1.0.0；发布构建会从 Git tag 注入真实版本。
DESKTOP_APP_VERSION=1.0.0

# Bundle Identifier，必须使用反向域名格式。
# 发布后不建议修改，否则会影响应用数据目录、升级识别和系统权限记录。
DESKTOP_APP_IDENTIFIER=xingyunxunzhi.desktop

# 文件名、命令名和发布产物使用的短名称。
# 只允许小写字母、数字和连字符。
DESKTOP_APP_SLUG=xingyunxunzhi-desktop

# 应用简短描述，用于安装包和系统应用信息。
DESKTOP_APP_DESCRIPTION=Local AI agent workspace

# 作者或维护者名称。多个作者使用英文逗号分隔。
DESKTOP_APP_AUTHORS=Xingyunxunzhi Contributors

# 项目仓库的公开访问地址，用于关于页面和帮助菜单。
# 留空时自动读取当前 Git origin，并在无法读取时使用项目内置仓库地址。
DESKTOP_APP_REPOSITORY=

# 唯一人工维护的源图标，相对项目根目录解析。
# 必须为正方形 PNG，至少 512 x 512，建议使用 1024 x 1024。
# 构建时由 app:sync 自动生成 macOS ICNS、Windows ICO 和平台尺寸 PNG。
DESKTOP_APP_ICON=src-tauri/icons/icon.png

# -----------------------------------------------------------------------------
# 内核来源
# -----------------------------------------------------------------------------

# 内核源码仓库。定制发行版可以替换为自己的 fork。
# harness:sync 必须实际从该仓库构建内核，不能只把地址写入来源说明。
HARNESS_REPOSITORY=https://github.com/xingyunxunzhi/xingyunxunzhi-harness.git

# 内核 tag、commit 或开发分支。本地开发留空时自动选择仓库中最新的 SemVer 版本标签。
# 社区版和正式发布还必须匹配 harness/toolchain-lock.json 中经过审计的固定提交。
HARNESS_REF=

# 可选的预构建签名制品通道。留空时按内核仓库准备源码候选；
# 配置后可使用 HTTPS、HTTP 或 file URL，清单与制品必须由下方发布者密钥签名。
HARNESS_UPDATE_MANIFEST_URL=

# stable 只接受稳定版本，preview 可接收预发布版本。
HARNESS_UPDATE_CHANNEL=stable

# 是否自动下载并在下次启动时安装。默认 false 表示发现后提醒，由用户决定是否下载；
# 显式填写 HARNESS_REF 时该值也会被强制关闭，防止开发联调内核被更新服务替换。
HARNESS_AUTO_UPDATE=false

# 可选签名制品通道的发布者身份。
HARNESS_UPDATE_PUBLISHER=xingyunxunzhi-desktop

# 可选签名制品通道的 Ed25519 原始公钥（Base64 编码的 32 字节）。
HARNESS_UPDATE_PUBLIC_KEY=

# -----------------------------------------------------------------------------
# 发行事实
# -----------------------------------------------------------------------------

# local、community 或 stable。普通本地构建保持 local；发布脚本会显式覆盖。
RELEASE_CHANNEL=local

# 安装包是否已经完成可信发布者签名。不要在未签名时填写 true。
RELEASE_SIGNED=false
